package q21;

import base.BinaryTreeNode;
import base.DataUtils;

import java.util.Stack;

/**
 * 创建一个栈，使 getMin() 、pop() 、 push() 的时间复杂度是 O(1)
 *
 *  在 Base->stack->q01
 * <p>
 * 思路：创建两个栈，一个保存元素，一个保存当前最小元素，这两个栈的元素始终一样，每保存一个元素的时候，最小元素都会和最小元素栈顶元素进行比较，插入小的那一个，也就是两个栈的长度始终相等
 */
public class AdvanceStack {

}
